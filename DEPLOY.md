# نشر المنصة (Production)

الإعداد ينشر خدمة واحدة: سيرفر Express يخدم الـ API وواجهة Vite المبنية على نفس الدومين
(لا حاجة لـ CORS ولا لضبط عنوان API في الواجهة).

## المتغيرات المطلوبة

| المتغير | النوع | القيمة |
|---|---|---|
| `DATABASE_URL` | runtime | رابط Postgres (يوفّره المزوّد تلقائياً) |
| `PORT` | runtime | يوفّره المزوّد (Express يقرأه) |
| `VITE_CLERK_PUBLISHABLE_KEY` | **build arg** | مفتاح Clerk العام — يُدمج في حزمة الواجهة وقت البناء |

## Railway (الأسهل مع Postgres)

1. New Project → **Deploy from GitHub repo** واختر هذا المستودع.
2. Railway يكتشف `Dockerfile` تلقائياً.
3. أضف **New → Database → PostgreSQL**؛ سيُضاف `DATABASE_URL` تلقائياً للخدمة.
4. في **Variables** أضف `VITE_CLERK_PUBLISHABLE_KEY` (Railway يمرّرها كـ build arg أيضاً).
5. **Settings → Networking → Generate Domain** للحصول على الرابط العام.

عند كل push جديد يعيد Railway البناء والنشر تلقائياً.

## Render

- New → **Web Service** → Runtime: **Docker**.
- أنشئ Postgres منفصلاً وانسخ `DATABASE_URL` إلى متغيرات الخدمة.
- أضف `VITE_CLERK_PUBLISHABLE_KEY` كـ **Environment Variable** ومع `--build-arg` في إعدادات الـ Docker build.

## أي VPS

```bash
docker build --build-arg VITE_CLERK_PUBLISHABLE_KEY=pk_live_xxx -t oman-debates .
docker run -d -p 80:3000 -e DATABASE_URL='postgresql://...' -e PORT=3000 oman-debates
```

## تثبيت التطبيق كـ PWA

- انشر الخدمة أولًا على رابط **HTTPS**؛ لا يكفي تشغيل نافذة المعاينة، ولم يُنشَر التطبيق تلقائيًا بهذه التغييرات.
- افتح الرابط المنشور مباشرة ثم اضغط **تثبيت التطبيق** في رأس الصفحة الرئيسية. يظهر طلب التثبيت الأصلي عندما يتيحه المتصفح، وإلا تظهر تعليمات مناسبة للجوال والكمبيوتر.
- على iPhone/iPad: Safari ← مشاركة ← إضافة إلى الشاشة الرئيسية. على Android: Chrome ← القائمة ← تثبيت التطبيق. على الكمبيوتر: Chrome أو Edge ← أيقونة التثبيت في شريط العنوان.
- يُفتح التطبيق المثبّت في نافذة مستقلة باسم «مناظرات عُمان» وبأيقونته الحالية؛ هذا ليس ملف APK ولا نشرًا في متاجر التطبيقات.
- عند انقطاع الشبكة تظهر شاشة عربية لإعادة المحاولة. تسجيل الدخول، تحميل البطولات وحفظ الدرجات **تحتاج اتصالًا بالإنترنت**؛ لم تُضَف مزامنة دون اتصال.
- عامل الخدمة يعمل في بناء الإنتاج فقط، ولا يخزّن استجابات `/api` أو صفحات الدخول أو بيانات البطولات. تُزال ذاكرة الإصدار القديم غير الآمنة بعد تفعيل العامل الجديد (قد يلزم إغلاق نوافذ التطبيق القديمة وإعادة فتحه).
- اجعل `/sw.js` قابلًا لإعادة التحقق (`Cache-Control: no-cache`) عند استخدام CDN، ولا تُعِد كتابة طلباته أو ملفات manifest والأيقونات إلى `index.html`.

### فحص ما بعد النشر

1. تحقق من `/manifest.webmanifest` و`/sw.js` و`/offline.html` و`/icon-192.png` و`/icon-512.png`؛ يجب أن تعيد الملفات الصحيحة وليست صفحة HTML بديلة.
2. افتح DevTools ← Application وتحقق من Manifest ومن تسجيل Service Worker تحت النطاق `/`.
3. جرّب التثبيت من متصفح يدعم PWA وتأكد أن التشغيل من الأيقونة يفتح نافذة مستقلة.
4. بعد تفعيل العامل، فعّل Offline من DevTools ثم أعد تحميل الصفحة: تظهر شاشة انقطاع الاتصال. أعد الشبكة واضغط «إعادة المحاولة».
5. تحقق أن Cache Storage لا يحتوي استجابات API أو بيانات دخول، وأن الصفحات الداخلية تفتح عند تحديثها مباشرة.

## ملاحظات

- مخطط قاعدة البيانات يُطبّق عند كل تشغيل عبر `drizzle-kit push` (موجود في `CMD`).
- بعد النشر تحقق من `https://<domain>/api/healthz` → يجب أن يعيد `{"ok":true}`.
- أضف دومين الإنتاج في لوحة Clerk (Allowed origins) وإلا لن يعمل تسجيل الدخول.
